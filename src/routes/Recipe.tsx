// Screen 3 — Recipe detail — spec §5.
// M0: placeholder. M1 will render the full detail layout.

export default function Recipe() {
  return (
    <main className="mx-auto max-w-md px-5 pt-12 safe-pt safe-pb">
      <h1 className="text-[17px] font-medium">Recipe detail</h1>
      <p className="mt-2 text-slate-500">
        Detail screen (M0 placeholder). Full layout in M1.
      </p>
    </main>
  );
}
