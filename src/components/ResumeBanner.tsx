// Global resume banner (spec §2).
//
// On every screen except cooking mode itself, if a cooking-state exists in
// localStorage and is <7 days old, render a non-blocking banner that lets
// the user resume or start fresh.
//
// M0: stub that always returns null. M3 will wire up the real logic.

export function ResumeBanner() {
  return null;
}
