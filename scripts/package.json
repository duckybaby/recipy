{
  "name": "recipy-scripts",
  "version": "0.0.0",
  "description": "One-off local scripts (backfill, migrations). Kept separate from functions/ so the function bundle stays lean.",
  "private": true,
  "scripts": {
    "backfill-library": "ts-node backfill-library.ts",
    "backfill-library:dry": "ts-node backfill-library.ts --dry-run"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  }
}
